<%- include("partials/header", {active:"activities"}) -%>
<div class="container">
  <h1>Fotoğraf Galerisi</h1>
  <hr>
  <br />


  <% for(var i = 0; i < gallery.length; i++) { %> <% if (i === 0 || i % 2
    === 0) { %>
      
  <div class="row">
    <% } %>
    <div class="col-md-6">
      <div class="card p-1" >
        <a href="/activity/<%= gallery[i].name  %> ">
        <img class="card-img-top" src="images/Gallery/<%= gallery[i].name %>/home.jpg" alt="Card image cap"
          height="350px"/>
        <div class="card-body">
          <h5 class="card-title" style="text-align: center"><%= gallery[i].name %></h5>
          
        </div>
      </div>
      </a>
    </div>
  
    <% if (i === 1 || i % 2 === 1) { %>
  </div>
  <% }%> <% } %>
  </div>

  <br>

  <%- include("partials/footer") -%>

